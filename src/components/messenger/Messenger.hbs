<section class="messenger">
    <aside class="messenger__sidebar">
    {{{ addChatForm }}}
    {{#if isAddUserVisible}}
        {{{ addUserToChat }}}
    {{/if}}
        <h1 class="messenger__title">Чаты</h1>
    {{{ chatList }}}
    <div class="messenger__sidebar-footer">
        {{{ changePasswordButton }}}
        {{{ changeAvatarButton }}}
        {{{ editProfileButton }}}
        {{{ logoutButton }}}
    </div>
</aside>
    <div class="messenger__chat">
        {{{ chatHeader }}}
        {{{ messages }}}
        {{{ sendMessageForm }}}
    </div>
</section>
